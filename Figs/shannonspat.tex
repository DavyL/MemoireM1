\documentclass[tikz]{standalone}
\usepackage{tikz, pgfplots}
\pgfplotsset{compat=1.16}
\usepgfplotslibrary{fillbetween}
\begin{document}
\begin{tikzpicture}[baseline]
	\begin{axis}[title = Ondelette $\psi$ de Shannon (temporelle),
		axis lines = left,
		scaled ticks = false,
		xlabel =$t$,
		ylabel = $\psi_{j, k}(t)$,
		legend entries={$\psi_{0,2}$,$\psi_{1,-1}$, $\psi_{2, 4}$},
		legend pos= north west,
		minor y tick num=0.5,
		minor x tick num=pi/2,
		trig format plots = rad,
		]
		
	\addplot[	red, 
				domain = -6.5:12,
				samples = 600]{ sin(2*pi*(x-2))/(pi*(x-2)) - sin(pi*(x-2))/(pi*(x-2))}; 	
	
	\addplot[	blue, 
				domain = -6.5:12,
				samples = 600]{ sqrt(2)*sin(2*pi*(2*x + 1))/(pi*(2*x +1)) - sqrt(2)*sin(pi*(2*x +1))/(pi*(2*x +1))}; 	

	\addplot[	brown, 
				domain = -6.5:12,
				samples = 600]{2*sin(2*pi*(4*x -32))/(pi*(4*x -32)) - 2*sin(pi*(4*x -32))/(pi*(4*x-32))}; 
	\end{axis}
\end{tikzpicture}
\end{document}
